import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    background: {
      default: 'lightgrey',
    },
    primary: {
      main: '#00897b',
    },
    secondary: {
      light: '#ffc107',
      main: '#ffc107',
    },
  },
})

export default theme
