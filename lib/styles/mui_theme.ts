import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "black",
    },
    primary: {
      main: "#00897b",
    },
    secondary: {
      main: "#ffc107",
    },
  },
});

export default theme;
